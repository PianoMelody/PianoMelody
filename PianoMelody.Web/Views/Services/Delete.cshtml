@model PianoMelody.Web.Models.ViewModels.ServiceViewModel

@{
    ViewBag.Title = Resources._Delete + " " + Resources._Service;
}

<h3>@Resources._AreYouSure</h3>
<hr />

<article class="panel panel-default">
    @Html.Partial("_ServiceInfo", Model)

     @if (User.IsInRole("Admin"))
     {
         <div class="panel-footer">
             @using (Html.BeginForm("Delete", "Services", new { returnUrl = this.Request.QueryString["returnUrl"] }, FormMethod.Post, new { }))
             {
                 @Html.AntiForgeryToken()

                 <div class="form-actions no-color">
                     <input type="submit" value="@Resources._Delete" class="btn btn-danger" />
                 </div>
             }
         </div>
     }
</article>
